public enum TokenType {
    VARIABLE,
    ID,
    NUMBER,
    STRING,
    NULL,
    DATA_TYPE,
    IN,
    EQUALS,
    SEMICOLN,
    DOT,
    FROM,
    WHERE,
    SELECT,
    FIRST_OR_DEFAULT,
    ORDER_BY_DESCENDING, ORDER_BY, DESCENDING, ASCENDING,
    MIN, MAX,
    LAMBDA_OPERATOR, OPERATOR, LOGICAL_OPERATOR, NEGATION,
    QUOTATION_MARK,
    OPENING_BRACKET, CLOSING_BRACKET,
    OPENING_CURLING_BRACKET, CLOSING_CURLING_BRACKET,
    OPENING_SQUARE_BRACKET, CLOSING_SQUARE_BRACKET
}
